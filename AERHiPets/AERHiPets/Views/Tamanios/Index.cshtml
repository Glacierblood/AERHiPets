@model IEnumerable<AERHiPets.Models.GestionAnimal.Tamanio>
<div class="row">
    <div class="col-lg-8 col-lg-offset-2 col-md-8 col-md-offset-2 col-sm-8 col-sm-offset-2 col-xs-12 fondo textoParrafo centrarContenido">
        @{
            ViewBag.Title = "Index";
        }

        <h2 class="textoTitulo"><strong>Lista de tamaños</strong></h2>

        <p>
            @Html.ActionLink("Nuevo Tamaño", "Create")
        </p>
        <div class="container">
            <div class="row">
                <div class="col-lg-10 col-md-10 col-sm-10 col-xs-10">
                    <table class="table" id="myTable">
                        <thead>
                            <tr>
                                <th class="col-lg-6 col-md-6 col-sm-6 col-xs-6">
                                    @Html.DisplayNameFor(model => model.nombre)
                                </th>
                                <th class="hidden-lg hidden-md hidden-sm hidden-xs">
                                    @Html.DisplayNameFor(model => model.descripcion)
                                </th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td class="col-lg-6 col-md-6 col-sm-6 col-xs-6">
                                        @Html.DisplayFor(modelItem => item.nombre)
                                    </td>
                                    <td class="hidden-lg hidden-md hidden-sm hidden-xs">
                                        @Html.DisplayFor(modelItem => item.descripcion)
                                    </td>
                                    <td class="col-lg-6 col-md-6 col-sm-6 col-xs-6">
                                        @Html.ActionLink("Editar", "Edit", new { id = item.Id }) |
                                        @Html.ActionLink("Detailles", "Details", new { id = item.Id }) |
                                        @Html.ActionLink("Eliminar", "Delete", new { id = item.Id })
                                    </td>
                                </tr>
                            }
                        </tbody>

                    </table>
                </div>

            </div>
        </div>
    </div>
</div>
